#!/usr/bin/sh

[ $# -ne 1 ] && exit 1 

case "$1" in
  "all")
    search_path="$HOME/mp3/"
    ;;
  "bandmaid")
    search_path="$HOME/mp3/bandmaid/"
    ;;
  *)
    exit 2
    ;;
esac

files=$(find "$search_path" -type f) && select=$(dmenu -l 20 <<< $files) && mplayer "$select"

